# Лабораторная работа №6

---

## Общая постановка задачи

Необходимо разработать консольную программу для учета и анализа продаж магазина.

Программа должна:

* хранить данные о продажах по дням
* обрабатывать продажи (расчет выручки)
* выполнять анализ (лучшие дни, товары)
* позволять изменять данные
* работать через консольное меню

---

## Исходные данные

Использовать следующую структуру:

```python
sales_data = {
    "Mon": [
        {"item": "Laptop", "price": 50000, "quantity": 1},
        {"item": "Mouse", "price": 1500, "quantity": 2},
    ],
    "Tue": [
        {"item": "Keyboard", "price": 3000, "quantity": 1},
        {"item": "Monitor", "price": 20000, "quantity": 1},
    ],
    "Wed": [
        {"item": "Mouse", "price": 1500, "quantity": 3},
        {"item": "Laptop", "price": 48000, "quantity": 1},
    ],
    "Thu": [
        {"item": "Headphones", "price": 7000, "quantity": 2},
        {"item": "Keyboard", "price": 3200, "quantity": 1},
    ],
    "Fri": [
        {"item": "Laptop", "price": 51000, "quantity": 1},
        {"item": "Monitor", "price": 21000, "quantity": 2},
    ],
    "Sat": [
        {"item": "Mouse", "price": 1400, "quantity": 5},
        {"item": "Headphones", "price": 7500, "quantity": 1},
    ],
    "Sun": [
        {"item": "Keyboard", "price": 2900, "quantity": 2},
        {"item": "Monitor", "price": 19500, "quantity": 1},
    ],
}
```

---

## Требования к функциям

---

### Общая выручка

```python
def total_sales(data):
```

**Требование:**
Вернуть общую выручку за все дни.

**Подсказки:**

* Нужно пройти по всем дням
* Внутри каждого дня пройти по продажам
* Для каждой продажи: `price * quantity`

---

### Выручка за день

```python
def sales_by_day(data, day):
```

**Требование:**
Вернуть выручку за указанный день.

**Подсказки:**

* Проверить наличие дня (`if day in data`)
* Использовать `sum()` или цикл

---

### Средняя выручка

```python
def average_daily_sales(data):
```

**Требование:**
Найти среднюю выручку за день.

**Подсказки:**

* Использовать `total_sales()`
* Разделить на количество дней (`len(data)`)

---

### Лучший и худший день

```python
def best_day(data):
def worst_day(data):
```

**Требование:**
Найти дни с максимальной и минимальной выручкой.

**Подсказки:**

* Для каждого дня считать выручку
* Сравнивать значения
* Можно хранить текущий максимум и день

---

### Крупные продажи

```python
def count_large_sales(data, threshold):
```

**Требование:**
Посчитать количество продаж, сумма которых превышает threshold.

**Подсказки:**

* `sale["price"] * sale["quantity"]`
* Использовать `if`

---

### Категории продаж

```python
def sales_category(amount):
```

**Требование:**
Вернуть категорию:

* < 5000 → low
* 5000–20000 → medium
* > 20000 → high

---

```python
def categorize_all_sales(data):
```

**Требование:**
Вернуть структуру с категориями по дням.

**Подсказки:**

* Создать новый словарь
* Использовать `sales_category()`

---

### Добавление продажи

```python
def add_sale(data, day, item, price, quantity):
```

**Требование:**
Добавить продажу.

**Подсказки:**

* Если дня нет – создать список
* Использовать `append()`

---

### Удаление продажи

```python
def remove_sale(data, day, item):
```

**Требование:**
Удалить продажу по названию товара.

**Подсказки:**

* Пройти по списку
* Найти совпадение по `item`
* Удалить один элемент

---

### Выручка по товарам

```python
def revenue_by_item(data):
```

**Требование:**
Вернуть словарь:

```python
{
    "Laptop": 150000,
    "Mouse": 9000,
}
```

**Подсказки:**

* Собрать данные по всем дням
* Если товара нет – добавить в словарь
* Если есть – увеличить значение

---

### Количество проданного товара

```python
def total_quantity_by_item(data, item):
```

**Требование:**
Сколько единиц товара продано.

---

## Главная программа

```python
def run_store_system(data):
```

---

### Меню:

```
1 – Общая выручка
2 – Лучший день
3 – Выручка по товарам
4 – Добавить продажу
5 – Удалить продажу
6 – Количество товара
0 – Выход
```

---

### Требования:

* Использовать `while True`
* Использовать `input()`
* Обрабатывать ввод пользователя
* Вызывать соответствующие функции

---

### Подсказки:

* `choice = input("Выберите действие: ")`
* `if choice == "1":`
* `break` для выхода

---

## Требования к коду

* Использовать функции
* Не дублировать код
* Понятные имена переменных
* Читаемая структура

---

## Ожидаемый результат

Программа должна работать как интерактивная система:

```
Выберите действие: 1
Общая выручка: 345000

Выберите действие: 3
Laptop: 149000
Mouse: 9000
```
